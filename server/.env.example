# Puerto HTTP del servidor (API + WebSocket + cliente en producción)
PORT=4000

# Credenciales del panel de administración
ADMIN_USERNAME=admin
ADMIN_PASSWORD=motocar123

# Archivo de base de datos (JSON) — se crea automáticamente
# En Northflank apunta a un volumen persistente montado en /data:
# DATA_FILE=/data/db.json
# DATA_FILE=./data/db.json
